using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class W8CapsuleScript : MonoBehaviour , W8IClickable
{
    // not actually using the capsule for this game - just keeping it around
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }

    public void OnClick()
    {
       // transform.localScale = new Vector3(2f, 2f, 0);
    }
}
